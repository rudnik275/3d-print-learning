# 2026-09-17 — snap-fit тест из Fusion (первая STL-модель через скилл `print-model`)

- Источник: Fusion «fit snap», экспорт STL Binary, One Body Per File (`~/Desktop/exports/`): Body1 — брусок 20 × 13 × 50 с двумя Т-пазами (канал 8 мм + перекладина 11 мм с круглыми концами, высота паза 8.4: один в середине, второй открыт с торца), Body2 — защёлка 12 × 17 × 12: блок-ручка и две консоли 2.5 × 8 × 7.5 с крючками Ø2.5. Зазор в дизайне 0.2 на сторону. Экспортированы в собранном положении (защёлка внутри среднего паза).
- Сборка — `build_snapfit.py` (через `bbs_calib.build`, контейнер пресетов `Box-skill-test-v3.3mf`): брусок повёрнут на 180° вокруг X — сплошная грань вниз, паз с торца открыт кверху, мостов внизу нет; защёлка как в Fusion, консоли в 2 мм над столом. Другая ориентация защёлки не годится: крючки вертикально — слои поперёк консоли, ломаются при первом защёлкивании. Брусок (100, 90), защёлка (124, 90). Проект `Snap-fit-test.3mf`, `settings-diff.txt`.
- Поверх `0.20mm Standard @BBL A1M` + `SUNLU PLA @BBL A1M`: `wall_loops 3`, `enable_support 1`, `support_type normal(auto)`, `support_on_build_plate_only 1`, `support_interface_spacing 0.2` (плотный интерфейс — низ консолей скользит по дну паза), ключи скилла (`resolution 0.004`, `slice_closing_radius 0.01`, `reduce_crossing_wall 1`, `max_travel_detour_distance 300`). `print_sequence` — by layer: брусок 50 мм выше зазора до балки (25), защёлка заканчивается на 12 мм.
- CLI: **33 мин + 6.5 подготовки, 8.4 г, 254 слоя**, предупреждений нет, брим Studio не добавила (auto). Старт Bambu (`M1002`), `S205` — 0. Файл `~/Downloads/Snap-fit-test.gcode.3mf` → Studio, Print plate.
- `mesh_slopes.py`: пологих граней 0 — ступенек не будет.
- Прогноз:
  - [заметно] низ консолей защёлки на поддержке (interface z 1.5–1.8) — шероховатая грань, она скользит по дну паза с зазором 0.2; плотный интерфейс сгладит, при тугом ходе снять заусенец.
  - [мелочь] потолок среднего окна 29.2–29.4 — мосты 8 и 11 мм, лёгкий провис, окно вверху чуть ниже 8.4.
  - [мелочь] сплошные слои под дном окна (20–20.6) и дном верхнего выреза (41–41.6): расход ×2.8 → возможна тонкая полоса на стенке.
  - [мелочь] кромки пазов 20.8 / 29.2 / 41.8 — контур меняет форму, шов aligned — небольшой горб со стороны паза.
  - [мелочь] шов на крючках Ø2.5; брусок весь на минимальном времени слоя (5.5 с → 6); без брима, база 260 мм², h/w 3.8.
  - Стоп-факторов нет. Поддержки 1.2 % пластика.
  - По самому тесту: пазы в PLA выходят на 0.1–0.2 уже номинала — посадка плотнее расчётной; не входит → зазор 0.3 в Fusion.
- Прогноз vs факт: —
