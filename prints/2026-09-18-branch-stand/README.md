# 2026-09-18 — подставка под телефон «ветви» (MakerWorld, 130 мм)

- Источник: `~/Downloads/Mobil_stand_branches_130_mm_tall.3mf` (автор JimTheSlim11, профиль под **H2S**: `0.16mm High Quality @BBL H2S`, четыре филамента Bambu PLA Matte/Basic под AMS, единственная авторская правка процесса — `enable_support 1`, tree(auto), порог 25°). Меш 1.9 М граней, раскраски нет, используется один экструдер. Масштаб автора 1.355 (до 130 мм) сидит в трансформации объекта.
- Сборка — `build.sh`: `retarget` на A1 mini / `0.16mm High Quality @BBL A1M` / `SUNLU PLA @BBL A1M` / Textured PEI → `move --at 100,90` (объект стоял в координатах H2S с центром (167, 161), на 180-м столе резался бы как «outside») → `variant --set`: ключи скилла (`resolution 0.004`, `slice_closing_radius 0.01`, `reduce_crossing_wall 1`, `max_travel_detour_distance 300`), зазоры поддержек `support_top_z_distance 0.2`, `support_object_xy_distance 0.8`, `enable_prime_tower 0`. `settings-diff.txt`. Проект 32 МБ в git не кладём — воспроизводится `build.sh` из скачанного файла.
- Ловушка переноса: `retarget` резал только списки `filament_*`, а авторский AMS оставил четыре значения в `filament_colour`, `filament_map`, `pressure_advance`, ключах вентилятора и матрице промывки 4×4 — Studio показывала четыре слота филамента на одно-филаментной пластине. Починено: все проектные списки длины авторского числа филаментов режутся до первого значения.
- CLI: **5 ч 48 мин + 7 подготовки, 67.5 г, 812 слоёв**, предупреждений нет, брима нет (auto), башни промывки нет. Файл `~/Downloads/Branch-stand.gcode.3mf` → Studio, Print plate. Studio при открытии спросила «Open as project / Import geometry only» (в окне был авторский проект) и «сохранить изменения?» — Open as project, No.
- `mesh_slopes.py --layer 0.16`: пологих граней 7.5 %, ступеньки на 0–20, 50–60, 90–100 мм.
- Прогноз:
  - [заметно] ступеньки на макушках лежащих веток (корни 0–20, 50–60, 90–100 мм), шаг до 0.9 мм; кора маскирует; лечит только 0.12 (+30 % времени) — оставлены авторские 0.16.
  - [заметно] верхушки 106–130 мм: 151 слой подряд < 6 с → глянец/оплывание концов Ø 3–5 мм; лечит башня-охладитель.
  - [заметно] следы интерфейса поддержек снизу веток (62 слоя, 1.3–100 мм); зазоры 0.2 / 0.8 для снятия.
  - [мелочь] шов aligned вдоль веток; внутренние мосты над заполнением основания (6.6 мм, «пролёт 74 мм» — это ширина зоны, под ней заполнение и сплошной слой); gap fill 0.1 %.
  - Стоп-факторов нет. Поддержки ≈ 6 % пластика, 42 мин.
- Прогноз vs факт (допечаталась 18.09):
  - ✓ ступеньки на пологих скатах — есть, Дима: «лесенки на горизонтальных поверхностях, где пирамидкой поднимается и опускается». Первый пункт прогноза, проверка 15 по мешу сработала. Решение: принять — декоративная модель, кора маскирует; 0.12 дал бы ступеньки на четверть мельче за +30 % времени.
  - верхушки веток, следы поддержек, снятие поддержек — ждут ответа.
