# 2026-09-16 — калибровка SUNLU PLA Bone White (пресет SUNLU PLA+ @BBL A1M)

- `flow-pass1.3mf` — Flow Rate coarse: 9 блоков −20…+20 % (`tools/bbs_calib.py flow1`, логика мастера Studio), `flow-pass1.gcode.3mf` — нарезка CLI, 27 мин, 7 слоёв. Раскладка 3×3, ряды сзади→вперёд: −20 −15 −10 / −5 0 +5 / +10 +15 +20 (цифры отлиты на блоках).
- Залит на SD-карту принтера: `/flow-pass1.gcode.3mf` и `/cache/flow-pass1.gcode.3mf`. Старт по MQTT отклонён: `project_file` → err_code 84033543 (свет через `ledctrl` управляется, печать — нет; нужен Developer Mode).
- Итог coarse → `flow2` (fine, −9…0 % поверх выбранного) → новый пресет филамента.
- `temp-tower-230-195.3mf` — башня температур из модели Bambu (`tools/bbs_calib.py temp`): вырезаны блоки 230→195 °C (подписи отлиты на задней грани, нижний блок самый горячий), смена температуры — custom G-code per layer внутри проекта, так что Studio при своей нарезке вставляет `M104` сама. 80 мм, 59 мин. `temp-tower-230-195.gcode.3mf` — CLI-нарезка, тоже залита на SD.
- Порядок: coarse расход (220 °C) → башня → fine расход на выбранной температуре → новый пресет филамента.
- `mvs-4-30.3mf` / `mvs-4-30.gcode.3mf` — Max Volumetric Speed: цилиндр ⌀40×60 в одну стенку (spiral vase), поток растёт линейно **4 → 30 мм³/с** от z = 0.4 до 60 мм (`tools/bbs_calib.py speed` + `speed-ramp`). Скорости зашиты в G-code — печатать **этот файл с SD** (Studio → Device → Storage, или экран принтера), не пересобирать. Чтение: высота h, где стенка начинает рваться/становится матово-рыхлой → поток = 4 + 26·(h − 0.4)/59.6; в профиль писать на ~2 мм³/с ниже. Ориентиры: 10 мм ≈ 8.2, 20 мм ≈ 12.6, 30 мм ≈ 16.9, 40 мм ≈ 21.3, 50 мм ≈ 25.6 мм³/с.
